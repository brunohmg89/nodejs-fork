pipeline {
    agent {
        docker {
            image 'node:alpine3.19'
            args '-p 3000:3000'
            args '-u root:root'
        }
    }
    stages {
        stage ('Build') {
            steps {
                sh 'npm install'
            }
        }
    }
}